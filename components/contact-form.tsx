"use client";
import { FormEvent, useState } from "react";
import { CheckCircle2, LoaderCircle } from "lucide-react";
export function ContactForm(){const [done,setDone]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget);const res=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:fd.get("name"),email:fd.get("email"),message:fd.get("message")})});if(res.ok)setDone(true);else{const data=await res.json();setError(data.message||"Pesan belum terkirim.");setBusy(false)}}if(done)return <div className="bg-white p-10 text-center"><CheckCircle2 className="mx-auto text-[#345348]" size={44}/><h2 className="mt-4 font-display text-3xl">Pesan sudah diterima.</h2><p className="mt-2 text-[var(--muted)]">Kami akan menghubungi Anda melalui email yang diberikan.</p></div>;return <form className="grid gap-5 bg-white p-6 md:p-9" onSubmit={submit}><label className="label">Nama<input required name="name" autoComplete="name" className="field"/></label><label className="label">Email<input required name="email" autoComplete="email" type="email" className="field"/></label><label className="label">Pesan<textarea required name="message" minLength={10} className="field min-h-40 resize-y"/></label>{error&&<p role="alert" className="text-sm text-red-700">{error}</p>}<button className="button" disabled={busy}>{busy&&<LoaderCircle className="animate-spin" size={17}/>}Kirim Pesan</button></form>}
