"use client";
import { FormEvent, useState } from "react";
import { LoaderCircle } from "lucide-react";
export function AdminLogin(){const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const res=await fetch("/api/admin/login",{method:"POST",body:JSON.stringify({token:form.get("token")}),headers:{"Content-Type":"application/json"}});if(res.ok)location.href="/admin";else{setError("Kredensial tidak valid atau AUTH_SECRET belum dikonfigurasi.");setBusy(false)}}return <form className="mx-auto max-w-md border border-black/15 bg-white p-8" onSubmit={submit}><label className="label">Access token<input className="field" name="token" type="password" required autoComplete="current-password"/></label>{error&&<p role="alert" className="mt-4 text-sm text-red-700">{error}</p>}<button className="button mt-6 w-full" disabled={busy}>{busy&&<LoaderCircle className="animate-spin" size={17}/>}Masuk</button></form>}
