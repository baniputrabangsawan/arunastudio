import type { MetadataRoute } from "next";
import { getPublishedPosts, getPublishedServices } from "@/lib/content-data";
import { getPublishedProjects } from "@/lib/portfolio-data";
import { siteUrl } from "@/lib/utils";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const [projects,services,posts]=await Promise.all([getPublishedProjects(),getPublishedServices(),getPublishedPosts()]);const updated=new Date("2026-08-24");const paths=["","/layanan","/portfolio","/harga","/simulasi","/estimasi","/mulai-project","/kontak","/blog","/privacy","/terms"];return [...paths.map(path=>({url:siteUrl(path),lastModified:updated,changeFrequency:path===""?"weekly" as const:"monthly" as const,priority:path===""?1:.7})),...services.map(s=>({url:siteUrl(`/layanan/${s.slug}`),lastModified:updated,changeFrequency:"monthly" as const,priority:.8})),...projects.map(p=>({url:siteUrl(`/portfolio/${p.slug}`),lastModified:updated,changeFrequency:"monthly" as const,priority:.7})),...posts.map(p=>({url:siteUrl(`/blog/${p.slug}`),lastModified:updated,changeFrequency:"monthly" as const,priority:.6}))]}
