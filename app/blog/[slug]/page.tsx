import { notFound } from "next/navigation";
import Link from "next/link";
import Image from "next/image";
import { ArrowLeft } from "lucide-react";
import { posts } from "@/lib/data";
export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}
export default async function PostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)notFound();const index=posts.findIndex(x=>x.slug===slug);const images=["/images/project-rasa-nusa.webp","/images/aruna-hero-business-owner.webp","/images/project-bengkel-selaras.webp"];return <main className="surface pb-24 pt-28"><article className="container max-w-3xl"><Link className="text-link" href="/blog"><ArrowLeft size={17}/> Semua artikel</Link><p className="eyebrow mt-16">{p.category} / {p.date}</p><h1 className="mt-5 text-5xl font-bold leading-[.98] tracking-[-.055em] md:text-7xl">{p.title}</h1><p className="mt-7 text-xl leading-relaxed text-[var(--muted)]">{p.excerpt}</p><div className="relative my-12 aspect-[16/9] overflow-hidden border border-black/20" data-parallax-viewport><div className="parallax-media-layer" data-parallax="media"><Image src={images[index]} alt="" fill priority sizes="(max-width: 768px) 100vw, 768px" className="object-cover"/></div></div><div className="prose prose-lg max-w-none"><p>Website yang baik membantu pelanggan memahami bisnis, menemukan jawaban, dan mengambil tindakan tanpa kebingungan. Desain harus dimulai dari perjalanan pelanggan, bukan dari daftar efek visual.</p><h2>Mulai dari pertanyaan pelanggan</h2><p>Susun informasi berdasarkan hal yang paling ingin diketahui calon pelanggan: apa yang ditawarkan, untuk siapa, berapa kisaran biayanya, dan bagaimana cara memulai. Struktur yang jelas sering memberi dampak lebih besar daripada fitur yang banyak.</p><h2>Tunjukkan alasan untuk percaya</h2><p>Tampilkan proses, scope, harga, dan karya secara jujur. Hindari angka, ulasan, atau urgensi buatan. Pengalaman digital perlu konsisten dengan cara bisnis berkomunikasi.</p><p>Artikel dapat dikelola melalui CMS tanpa mengubah source code.</p></div></article></main>}
